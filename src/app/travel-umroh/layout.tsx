import { metadata } from './metadata'

// Export metadata
export { metadata }

export default function TravelUmrohLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
